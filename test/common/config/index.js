'use strict';

const path = require('path');

module.exports = {
    redis: {
        host: '127.0.0.1',
        port: 6379,
    },
    monitor: {
        enabled: true,
        host: '127.0.0.1',
        port: 3000
    },
};